# Manual vs Neat Framework: Payment Processing Comparison

This file demonstrates identical payment processing implementations using:
1. Manual Factory + Strategy pattern
2. Neat Framework decorators

Proves: Neat provides same flexibility + more features with less code

```typescript
import { Injectable, Strategy, FactoryPattern, StartupApplication } from '../core/src/decorators/index.js';
import { brandPort } from '../core/src/types/branded.js';

// ... rest of the comparison code ...
```

See the original file for complete implementation details.

